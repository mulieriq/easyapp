import 'package:flutter/material.dart';

class ViewProducts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: new Center(
        child: new Text('List Of Product'),
      ),
    );
  }
}
